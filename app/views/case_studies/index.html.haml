%section#breadcrumb
  = render "feature_categories/breadcrumb"
.container
  %section#content
    -if @page.present?
      = render :partial => "pages/page_content_header"
    - for case_study in @case_studies do
      %section{:id => case_study.slug, :class => "case_studies left_aligned"}
        - if case_study.assets.present?
          %img{:src => case_study.assets.first.asset.url(:small), :alt => case_study.assets.first.description}
        .wrapper
          %h2
            = case_study.case_study_category_name
            %span= "- #{link_to case_study.name, case_study}".html_safe

          %p= case_study.summary
          %p.read_more= link_to "More", case_study

%section#prev_next_bar
  &nbsp;
%section#related
  = render "pages/related"