%section#breadcrumb
  = render "breadcrumb"
.container
  %section#sidebar
    = render "sidebar"
  %section#content
    %h1= @page.name
    %h1= @layout_name

    = render :partial => "/pages/#{@page.layout}", :locals => {:page => @page}

%section#prev_next_bar
  = render "prev_next_bar"
%section#related
  = render "related"