%section#breadcrumb
  = render "pages/breadcrumb"
.container
  %section#sidebar
    = render "sidebar"
  %section#content
    %header
      %h1.underlined Downloads
    - for download in @downloads do
      = render "download_items", :download => download

%section#prev_next_bar
  &nbsp;
%section#related
  = render "pages/related"